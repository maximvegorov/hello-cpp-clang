{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug CMake target (lldb)",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "env": {},
      "preLaunchTask": "CMake: build",
      "stopOnEntry": false
    }
  ]
}
